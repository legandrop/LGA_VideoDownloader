v0.96:

        - `INSTALADOR / YA NO SE EJECUTA SOLO SIN CONSOLA` `instalador.bat` terminaba en un `choice` que pregunta si ejecutar el instalador. Corrido sin consola (encadenado o con la entrada redirigida), el `choice` leia la respuesta de la entrada: con una `Y` en el pipe ejecutaba el instalador recien armado, y el codigo de salida era el del `choice`. Ahora, sin consola interactiva no se ejecuta y se avisa; `--no-run` lo omite siempre, y el script termina con `exit /b 0` explicito. Con consola la pregunta sigue igual. Ademas se corrige un comentario de `compilar.bat` que tenia una tabulacion en lugar de `build\tools`. [ Instalador - Ya no se ejecuta solo sin consola ]

        - `INSTALADOR / CIERRA LA APP POR RUTA, NO POR NOMBRE` El `PrepareToInstall` del instalador hacia `taskkill /F /IM` de `VideoDownloader.exe` y `VimeoDownloader.exe`, que cierra cualquier copia con ese nombre: un build, otro checkout, una copia portable en otra carpeta. Ahora el instalador lleva `tools/close_by_path.ps1`, lo extrae a `{tmp}` y cierra solo lo que corre desde la carpeta de destino o desde la instalacion legacy `C:\Portable\LGA\VimeoDownloader`. El instalador del auto-update, que corre desde `{app}\updates`, queda afuera por nombre. Si el script no puede correr, no cierra nada e Inno avisa "archivo en uso". Probado con una simulacion de las dos llamadas sobre procesos de prueba; falta compilarlo con Inno. [ Instalador - Cierra la app por ruta, no por nombre ]

        - `UPDATES / EL INSTALADOR DEL AUTO-UPDATE SE BAJA DENTRO DE LA INSTALACION` El instalador que baja el auto-update (~80 MB) iba a `%TEMP%\LGA_VideoDownloader_updates`, fuera de la instalacion: quedaba ahi hasta el proximo arranque, y para siempre si se desinstalaba justo despues. Ahora en Windows va a `<app>\updates`, con fallback a `%LOCALAPPDATA%\LGA\VideoDownloader\updates` si la carpeta de la app no acepta una escritura real. Al arrancar, y otra vez a los 3 minutos, se barren los instaladores viejos de esas carpetas y de la de `%TEMP%`; las que quedan vacias se borran. El desinstalador borra `{app}\updates`. macOS no cambia. Nuevo `--qa-update-dirs` para probarlo sin red. En el proximo release, mirar que `{app}\updates` quede vacia despues de actualizar. [ Updates - El instalador del auto-update dentro de la instalacion ]

        - `HELP / LA VERSION DE FFMPEG CORTABA SU FILA` En la ventana de Help la fila de FFmpeg se veia cortada: "Merges audio and vid" y la version truncada. El ffmpeg que se distribuye es un build nocturno y su version (`N-117208-gbd22d7e601-20240927`, 203 px) no entra en la fila de ancho fijo, asi que el rol y la version quedaban en 137 px cada uno. La captura de QA usaba `7.1-full_build` y nunca lo mostro. Ahora los nocturnos se muestran por su fecha (`2024-09-27`), cualquier version de mas de 16 caracteres se recorta, y la cadena completa queda en el tooltip. `--ui-shot help` usa la version real y su `.json` anota el ancho que pide cada texto (`hintWidth`), para detectar recortes. [ Help - La version de FFmpeg cortaba su fila ]

        - `SCRIPTS / COMPILAR Y DEPLOY YA NO CIERRAN LA APP INSTALADA` `compilar.bat` y `deploy.bat` hacian `taskkill /F /IM VideoDownloader.exe`, que cierra por nombre todas las copias: cada build cerraba tambien la app instalada y le cortaba las descargas en curso. Ahora cierran por la ruta real del proceso con `tools/close_by_path.ps1`, copia sin cambios de la pieza de LGA_Base_QT_C_Py (rev 2): `compilar` solo cierra `build\VideoDownloader.exe`, y `deploy` ademas lo que corra desde `deploy\`, antes de borrar esa carpeta. Los dos suman `--no-run` para compilar sin abrir la app. [ Scripts - Compilar y deploy no cierran la app instalada ]

v0.95:

        - `TOOLS / LA MIGRACION SOLO ACEPTA UNA CARPETA DE TOOLS` La migracion borra la carpeta de origen cuando termina, y no verificaba que esa carpeta fuera de tools: apuntada a una carpeta cualquiera, se la llevaba puesta con todo lo que tuviera adentro. Solo se podia llegar ahi por la bandera de prueba `--qa-migrate`, que igual viaja en el binario publicado. Ahora se exige que el origen tenga `yt-dlp.exe`, `deno.exe` o `tools.json` (sueltos o en `.staging`); si no, no se toca nada y la bandera sale con error explicando por que. [ Tools - La migracion solo acepta una carpeta de tools ]

        - `TOOLS / LA MIGRACION NO PUEDE PERDER UN BINARIO` La migracion de la tanda anterior confiaba en `tools.json` para decidir: si esa entrada faltaba, estaba truncada o traia una version ilegible, la tool se salteaba pero la carpeta vieja se borraba igual, asi que el binario desaparecia. Con deno era grave: pesa 97 MB, no viene en el instalador y sin el no anda YouTube. Ahora decide el binario, no el archivo de estado: se le pregunta `--version` a la tool de origen y, si no contesta, no se mueve ni se borra. Tambien se migra lo que haya en `.staging` con su entrada, para no tirar una descarga ya verificada. La carpeta anterior se borra SOLO si no quedo nada atras; si algo estaba en uso, se reintenta al proximo arranque. Ademas se barren las cookies temporales viejas y resolver una ruta ya no crea carpetas al pasar. [ Tools - La migracion no puede perder un binario ]

        - `TOOLS / TODO LO PESADO DENTRO DE LA CARPETA DE LA APP` En Windows las tools quedaban duplicadas y desparramadas: el instalador dejaba un juego en `<app>\tools` y el auto-update bajaba otro a `%LOCALAPPDATA%\LGA\VideoDownloader\tools` (yt-dlp 18 MB + deno 97 MB), asi que la instalacion no era una sola cosa y al desinstalar quedaban restos. Ahora las tools y las cookies temporales de la extension viven en la carpeta del ejecutable, la misma que siembra el instalador, y se actualizan ahi mismo; si esa carpeta no es escribible se cae a LOCALAPPDATA, detectado con un intento real de escritura. Al arrancar se mueven las tools mas nuevas que hayan quedado en LOCALAPPDATA y se borra esa carpeta. El instalador ya no pisa yt-dlp ni deno, y al desinstalar borra `{app}\tools` y `{app}\session-cookies`. En macOS no cambia nada: siguen fuera del bundle, porque escribir adentro rompe la firma. [ Tools - Todo lo pesado dentro de la carpeta de la app ]

v0.94:
        - `EXTENSION / BOTON EN EL NAVEGADOR CON LA SESION, E INSTANCIA UNICA` Para bajar algo habia que copiar el link y, si pedia sesion, elegir un navegador cuyas cookies Windows no deja leer. Ahora la extension de `extension/` manda la pagina por Native Messaging: el navegador lanza el mismo exe con `chrome-extension://<ID>/`, que valida el pedido y lo pasa a la app abierta (o la abre). Las cookies llegan estructuradas; la app arma un cookies.txt temporal por descarga en `%LOCALAPPDATA%`, lo borra al terminar y barre restos al arrancar, y Retry las conserva. La app pasa a ser de instancia unica. El instalador registra la clave de Chrome (la leen Brave y Edge), la app la repara si falta, y Help explica como cargar la extension. `--qa-cookies` prueba el cookies.txt. macOS no verificado en Mac. [ Extension - Boton en el navegador con la sesion ]

v0.93:
        - `DESCARGAS / AJUSTES DE LA AUDITORIA: LINKS, SOUNDCLOUD, PROGRAMADOS Y MP4` Varios bordes de la tanda anterior. SoundCloud avisa siempre "...account credentials" como WARNING y cualquier fallo se leia como falta de sesion: ahora se clasifica solo con lineas ERROR. El texto pegado cortaba links en las comas (`?ids=1,2,3`), dejaba `)` o `!` finales, ignoraba `archive.org/...` sin esquema y repetia el mismo video pegado como `watch?v=`, `youtu.be` o `shorts`. Ahora la coma solo separa antes de otro link, la puntuacion final se recorta si no esta balanceada, se aceptan host con TLD y path, y se deduplica por id de YouTube/Vimeo. Un vivo programado dice "This live stream hasn't started", y una playlist con un vivo queda completa con aviso. "Most compatible" prefiere mp4 (archive.org bajaba .ogv). [ Descargas - Ajustes de links, SoundCloud y mp4 ]

        - `DESCARGAS / CUALQUIER SITIO, VIVOS, TEXTO PEGADO Y PARCIALES AL CANCELAR` Pruebas reales con la app instalada: texto con espacios creaba una tarjeta por palabra, un vivo de YouTube fallaba a los 20 s con un codigo crudo de ffmpeg, cancelar dejaba el `.part` y solo se aceptaban YouTube y Vimeo. Ahora `LinkParser` extrae solo los links (sin duplicados) y el texto suelto da una unica tarjeta "No link found"; se acepta cualquier sitio de yt-dlp, con chip por sitio o extractor, "This site isn't supported" y audio para sitios solo de audio. Los vivos se saltean con `--match-filter` ("Live streams aren't supported") y al cancelar o cerrar se borran los parciales de esa descarga. Los errores de sesion nombran al sitio. No se reescribe Vimeo al player: sin sesion sus formatos salen con DRM. Docs al dia. [ Descargas - Cualquier sitio, vivos y parciales al cancelar ]

v0.92:
        - `RELEASE / ACTUALIZACION AUTOMATICA VALIDADA DESDE LA APP` La prueba de la 0.91 no pudo hacerse desde la app: el firewall local retenia la conexion del ejecutable recien instalado y el instalador se corrio a mano con los mismos argumentos. La 0.92 no agrega funciones: existe para que una 0.91 instalada, ya con acceso a red, recorra el circuito completo por si sola (aviso de version nueva, descarga del instalador, verificacion del hash, instalacion silenciosa y relanzamiento). [ Release - Actualizacion automatica validada desde la app ]

v0.91:
        - `RELEASE / PRIMERA ACTUALIZACION AUTOMATICA PUBLICADA` La 0.90 es la primera version publicada en GitHub Releases con el instalador y su `SHA256SUMS`, y el auto-update de la app solo se puede probar de punta a punta si existe una version posterior. La 0.91 no agrega funciones: existe para validar el circuito completo desde una 0.90 instalada (detectar el release nuevo, bajar el instalador, verificar el hash, instalar en silencio sobre la misma carpeta y relanzar). [ Release - Primera actualizacion automatica publicada ]

v0.90:
        - `UI / VENTANA BAJA SIN CONTROLES SUPERPUESTOS Y CORRECCIONES DE LA AUDITORIA` Con poco alto (notebook de 768) la tarjeta "Add videos" se aplastaba y sus controles se pisaban, y el Retry de una tarjeta de error quedaba escondido. Causa: la tarjeta aceptaba comprimirse, el log tenia alto fijo y el minimo de ventana era una constante. Ahora la tarjeta conserva su alto, el log cede primero hasta 4 lineas, la cola siempre muestra una tarjeta de error completa, el minimo sale de los layouts (666px) y la ventana no abre mas alta que el area de trabajo. Ademas: un fallo cuenta un solo error, recortar el log no mueve lo que se esta leyendo, UTF-8 partido entre lecturas ya no se rompe, el progreso pondera video y audio por bytes, "Retry failed" respeta las opciones de cada item y "Use cookies from" se marca en rojo ante un error de sesion. [ UI - Ventana baja y correcciones de la auditoria ]

        - `UPDATES / DENO NO SE INSTALABA CON RUTAS UNICODE Y LOS INSTALADORES QUEDABAN EN TEMP` Si la carpeta del usuario tenia caracteres fuera del code page ANSI (`日本`, `Ω`), deno nunca se instalaba: `tar.exe` recibe sus argumentos en ANSI y la ruta absoluta del zip le llegaba rota. Ahora se lanza con la carpeta de extraccion como working directory y rutas relativas (`../dl/<zip>`, `-C .`). Ademas cada update de la app dejaba su instalador (~80 MB) en `%TEMP%\LGA_VideoDownloader_updates` para siempre: al arrancar y antes de bajar uno nuevo se borran los instaladores viejos de esa carpeta. Correccion a la entrada "Auto-update de tools y de la app": `UpdateService` ya no lee el manifiesto de LGA_Updates sino el ultimo release de GitHub. En las reglas, el bullet de `dmgbuild` vuelve a "Deploy de macOS" y se documentan `--ui-shot` y `--qa-walkthrough`. [ Updates - Deno con rutas Unicode y limpieza de instaladores viejos ]

        - `UI / REDISENO: TARJETAS DE COLA, LOG SIEMPRE VISIBLE Y AYUDA CON UPDATES` La ventana era un formulario de un solo link con Settings y Log plegables, una barra de progreso para toda la cola y el update escondido en un boton provisorio. `mainwindow.cpp` mezclaba todo y `DownloadQueue`/`ToolsManager` escribian directo en widgets. Ahora hay pestaña DOWNLOADS con tarjeta "Add videos" (varios links, MP4 o solo audio M4A, calidad compatible H.264 por defecto, navegadores detectados para las cookies), una tarjeta por link con progreso real y errores explicados con su solucion, log con niveles y filtros, y dialogo de Help con version, updates, versiones de las tools y creditos. La cola y las tools pasan a emitir senales; Inter va embebida. Se suma `--ui-shot` para capturar estados sin abrir ventanas. [ UI - Rediseno de la ventana principal ]

        - `UPDATES / LA APP BUSCA SU VERSION EN GITHUB Y CANCELAR YA NO DEJA YT-DLP HUERFANO` El update de la app dependia de que el repo estuviera en el manifiesto de LGA_Updates: sin eso decia "al dia" en silencio. Ahora lee el tag del redirect de `releases/latest`, como las tools, y exige el hash del asset en un `SHA256SUMS` del mismo release; `instalador.bat` y `deploy.sh` lo generan. Ademas, cancelar, reiniciar la cola o cerrar la app mataba solo el lanzador de yt-dlp: el ejecutable onefile corre el yt-dlp real como hijo, que seguia descargando. Ahora se mata el arbol completo (`taskkill /T` en Windows, grupo de procesos en macOS). [ Updates - Version de la app desde GitHub y kill del arbol de yt-dlp ]

        - `UPDATES / YT-DLP Y DENO SE ACTUALIZAN SOLOS Y LA APP AVISA SUS VERSIONES NUEVAS` Cuando YouTube cambiaba, yt-dlp quedaba viejo hasta que alguien tocaba "Update dlp", que bajaba a RAM y escribia directo sobre el binario de la instalacion, sin verificar nada, y re-bajaba deno cada vez. Ahora `ToolsUpdater` instala y actualiza yt-dlp y deno al abrir la app, desde GitHub con tag fijo y SHA-256 obligatorio, en la carpeta de datos del usuario, y el reemplazo se hace solo cuando no hay un yt-dlp corriendo. `UpdateService` lee el manifiesto de LGA_Updates: en Windows baja el instalador verificado, corta las descargas con sus procesos hijos e instala sobre la misma carpeta; en macOS abre el release. `instalador.bat` toma la version de `VERSION` y relanza la app en modo silencioso. Sin login elegido ya no se usan cookies en ninguna plataforma, y se borra el `Downloader` muerto. [ Updates - Auto-update de tools y de la app ]

        - `LOGIN / COOKIES DEL NAVEGADOR EN VEZ DE USUARIO Y CONTRASENA, Y DENO EN WINDOWS` YouTube fallaba en Windows con "No supported JavaScript runtime" y "Sign in to confirm your age". Causas: deno solo se instalaba y pasaba con `--js-runtimes` en macOS, y la app pedia mail y contrasena de Vimeo, que ademas guardaba en texto plano en `config.ini`. Ahora Windows baja `deno.exe` a `tools/` desde "Install Tools"/"Update dlp" y lo pasa a yt-dlp; el login es un selector de origen de cookies (`--cookies-from-browser` o `--cookies` con un cookies.txt), y al arrancar se borra el grupo `[vimeo]` del `config.ini` actual y del legacy. Los fallos conocidos dejan un `HINT` en el log: Chrome/Brave abiertos bloquean su base, Edge no se puede descifrar (DPAPI), falta sesion. stderr ya no se marca entero como `ERROR:` y se va el `User:` vacio. `compilar.bat` deja de pisar con el del repo el yt-dlp actualizado en `build/tools`. [ Login - Cookies del navegador en vez de credenciales y deno en Windows ]

        - `REGISTRO / LA APP SE AUTO-REGISTRA EN EL REGISTRO COMPARTIDO DE LGA` al arrancar deja su nombre, version, `installPath` y ejecutable en `~/Library/Application Support/LGA/VideoDownloader.json` (`%APPDATA%/LGA/` en Windows), que es como las apps LGA se enteran unas de otras — el primer consumidor es el card de LGA Updates de PipeSync, que hasta ahora no tenia forma de saber donde estaba instalada esta app. Entra `src/utils/LgaRegistry.cpp` con su header, copiado tal cual del template. La llamada va DESPUES de `migrateLegacyAppDataDir()`, para no tocar AppData antes de que se mueva la carpeta vieja, y despues de `setApplicationName` porque el registro se resuelve subiendo desde `AppDataLocation` y los dos tienen que coincidir. NO se registra corriendo desde una salida de desarrollo —carpeta contenedora llamada `build`/`deploy`, o adentro del arbol de build o del repo—: como se escribe en cada arranque, el ultimo binario abierto gana, y sin eso el build le pisaria al card la ruta de la copia instalada. [ Registro - La app se auto-registra en el registro compartido de LGA ]

        - `TOOLING / sync_version --check ESCRIBIA EL ARCHIVO EN VEZ DE VERIFICAR` `tools/sync_version.py` parseaba el flag a mano con `"--check-only" in sys.argv[1:]`, asi que cualquier otro flag —incluido `--check`, que es el que usan LGA_RepoTools, FileManagerS3 y LinkRedirector— **se ignoraba en silencio y caia en la rama que escribe**, reescribiendo `VERSION`. No es hipotetico, es como se descubrio: una auditoria corrio `--check` esperando algo de solo lectura. Un flag desconocido ahora **corta con codigo 2** en vez de degradar a la accion destructiva, y `--check` queda como alias de `--check-only` para que el mismo comando haga lo mismo en los seis repos; `--check-only` se mantiene porque lo usa el flujo de release. Verificado con md5 antes/despues sobre una copia con el `VERSION` desincronizado. Se corrige ademas el comentario del candidato de macOS en el `HOOK`, que decia que el problema era `bin/python3` cuando el propio `python3` **es un symlink a `bin/python3.10`** y sufre lo mismo. [ Tooling - sync_version --check ya no escribe el archivo ]

        - `TOOLING / EL HOOK DE SYNC_RULES NUNCA PUDO CORRER EN WINDOWS` la regla decia "HOOK: corriendo en mac, FALTA EN WINDOWS", y se leia como que nadie lo habia instalado. No era eso: el `pre-commit` elegia el interprete con `command -v python3`, y en Windows `python` y `python3` **existen en el PATH** como alias del Microsoft Store —imprimen "Python was not found" y salen con 49—, asi que `command -v` los daba por buenos y el hook moria siempre. Ahora el hook y los wrappers **prueban cada candidato corriendolo**, en orden: el `python_runtime/` del propio repo, el de `../LGA_FileManagerS3`, `py -3`, y recien despues el del PATH. La idea de fondo es no depender de un Python global, que taparia el dia que una app portable deja de andar con su runtime embebido. Si no hay ninguno, el hook avisa y **deja pasar** el commit: bloquear por una dependencia faltante es peor que no verificar. Se suma `__pycache__/` al `.gitignore`, que faltaba. Hook ya instalado de los dos lados; el `pre-commit` de los siete repos LGA quedo byte a byte identico. No se toca la version: el changelog ya estaba por encima del `CMakeLists.txt`. [ Tooling - El hook de sync_rules ya corre en Windows ]

        - `RENAME MACOS / LA APP PASA A LLAMARSE "LGA Video Downloader"` convencion LGA: el nombre visible arranca con `LGA` para que todas las apps queden juntas en `/Applications`, donde Finder ordena por el nombre de ARCHIVO del bundle y no por `CFBundleName`. En un target `MACOSX_BUNDLE` el `OUTPUT_NAME` define el `.app` Y el ejecutable, asi que los dos pasan a llamarse `LGA Video Downloader`; los artefactos van sin espacios (`LGA_Video_Downloader_Mac_vX.zip`) porque viajan por URL en los releases. El rename va solo en el bloque `if(APPLE)`. Dos bugs que encontro una auditoria y que dejaban artefactos rotos: **(1)** `mkdir -p "deploy/...app/Contents/{MacOS,Resources,Frameworks}"` — las comillas, agregadas por los espacios del nombre nuevo, DESACTIVAN la expansion de llaves, asi que se creaba un unico directorio literal `{MacOS,Resources,Frameworks}` y el binario nunca llegaba al bundle; el `.zip` y el `.dmg` se generaban igual, con 137 MB de frameworks y sin la app, y el DMG hasta pasaba la verificacion porque esta chequea el fondo y no que el bundle sea ejecutable. **(2)** `cmake/Info.plist.in` tenia `CFBundleExecutable` como LITERAL `VideoDownloader`, ignorando la propiedad `MACOSX_BUNDLE_EXECUTABLE_NAME` que el CMakeLists ya seteaba —la misma clase de bug que el del versionado—, asi que el bundle de dev no arrancaba por Finder. Se corrigen ademas el workflow de CI de macOS, el instalador legacy y las reglas del repo. Verificado: el bundle arranca, el DMG monta con firma valida. [ Rename - La app pasa a llamarse LGA Video Downloader en macOS ]

        - `DEUDA / EL DEPLOY PISA EL Info.plist QUE GENERA CMAKE, Y HAY QUE MANTENER DOS COPIAS` queda anotado en el codigo, en las dos puntas. `deploy.sh` reescribe el `Info.plist` entero con un heredoc, encima del que CMake ya genero desde `cmake/Info.plist.in`: son dos copias de la misma informacion y arreglar una no arregla la otra. Es lo que causo el bug del versionado de la entrada anterior —se corrigio `CFBundleShortVersionString` en una y `CFBundleVersion` quedo en `0.86` en la otra—, y el bundle publicado salio con las dos claves discrepando durante tres versiones. El heredoc existe porque agrega claves de compatibilidad que el template no trae (`LSMinimumSystemVersion`, `LSArchitecturePriority`, `NSAppTransportSecurity`, `NSSupportsAutomaticGraphicsSwitching`); lo correcto es moverlas al template y que el deploy no toque el plist, pero eso pide una pasada con cuidado y no entra en este cambio. Por ahora las dos copias estan sincronizadas y cada una avisa de la otra. [ Deuda - Se documenta que el deploy pisa el Info.plist generado por CMake ]

        - `VERSIONADO / EL NUMERO SE ESCAPABA POR TRES LUGARES Y EL BUNDLE DECIA 0.86` el repo declaraba bien la fuente de verdad —`project(VideoDownloader VERSION ...)` expuesto como macro— pero el numero estaba escrito a mano en tres lados. El `cmake/Info.plist.in` hardcodeaba `0.86` e IGNORABA las propiedades `MACOSX_BUNDLE_BUNDLE_VERSION` que el `CMakeLists` ya seteaba desde `PROJECT_VERSION`, o sea que esas propiedades estaban muertas. El heredoc de `deploy.sh` que reescribe el plist entero usaba la variable para `CFBundleShortVersionString` pero dejaba `CFBundleVersion` en `0.86`: el bundle publicado salia con las dos claves discrepando, tres versiones atras. Y `mainwindow.cpp` ponia `v0.89` a mano en el titulo de ventana. Los tres pasan a derivar de la macro. Se suma ademas el archivo `VERSION` con su `sync_version.sh`/`.bat`, para que el repo tenga la misma forma que el resto de las apps LGA: es un espejo DERIVADO del CMakeLists, no la fuente, y `--check-only` avisa si quedan desincronizados. Verificado sobre el bundle deployado: las dos claves del plist dicen 0.89 y el titulo tambien. [ Versionado - El numero sale solo del CMakeLists y se suma el VERSION derivado ]

        - `DMG / SE SUMA EL DMG DE INSTALACION, CON LA CADENA DEL TEMPLATE` `deploy.sh` solo producia el `.zip` de actualizacion, asi que no habia artefacto de primera instalacion. Se suma `create_dmg.sh` y el flag `--dmg` al deploy. El DMG usa el diseno del template (`../LGA_Base_QT_C_Py/docs/Doc_Deploy_macOS.md`): ventana dark, iconos de 96, titulo y flecha entre la app y `Applications`. Resuelve tres restricciones de Finder medidas en macOS 26: no estira el background —mas alla de la imagen rellena en BLANCO, tambien en Dark Mode, de ahi el lienzo gigante de color plano—, con background propio pinta los nombres en NEGRO —de ahi la placa clara bajo cada label— e ignora el tamano de ventana guardado. El layout lo escribe `dmgbuild` y NO AppleScript, que devuelve exito sin escribir el fondo, y el script verifica sobre el archivo producido que el `.DS_Store` tenga `backgroundImageAlias`. No instala nada: `dmgbuild` va vendorizado en `tools/macos/vendor/` y el fondo lo genera `make_dmg_background.js` con AppKit via JXA. El volumen monta como `LGA Video Downloader`; el archivo sigue siendo `VideoDownloader_Mac_v<version>.dmg`. [ DMG - Se suma el DMG de instalacion con la cadena del template ]

        - `REPO / EL .gitattributes SE ALINEA CON LA POLITICA CANONICA LGA` la politica del repo ya estaba bien planteada pero cubria pocas extensiones: `.py`, `.qml`, `.ui`, `.cmake`, `.yaml`, `.xml` y `.svg` no estaban declaradas y quedaban a merced de `text=auto`, y del lado binario faltaban `.jpg`, `.ttf`, `.zip`, `.dylib` y los contenedores de video, que se corrompen si Git llega a tomarlos por texto. Se adopta el bloque canonico comun a los repos LGA —documentado en `LGA_Base_QT_C_Py/docs/Doc_Line_Endings.md`— y se suma `.ahk` al grupo de scripts nativos de Windows. Las exclusiones de `tools/` y `toolsmac/` quedan tal cual, con su comentario: los binarios de macOS no tienen extension, asi que ninguna regla por extension los cubre y hay que excluir el arbol entero. [ Repo - Alinear el .gitattributes con la politica canonica LGA ]

        - `DEPLOY MACOS / EL BUNDLE NO ERA AUTOCONTENIDO Y NO SE EMPAQUETABA` `deploy.sh` salteaba `macdeployqt` con la nota de que "la app funciona si Qt esta en el sistema destino", o sea que el `.app` linkeaba contra `/opt/homebrew/opt/qtbase` y solo corria en una maquina con ese mismo Qt. Ahora corre `macdeployqt` y despues `tools/macos/bundle_fixup.py`, que completa lo que la herramienta no persigue con los kegs de Homebrew: faltaban `QtDBus` —sin ella el binario muere en `dyld`— y su `libdbus-1`, mas `QtSvg`, `QtPdf` y las dos `QtVirtualKeyboard`, tres de ellas declaradas como `@rpath` pero nunca copiadas. El script verifica que no quede ninguna referencia fuera del bundle y corta el deploy si queda. Se suman firma ad-hoc y `--zip` (con `ditto`, nunca `zip -r`, que resuelve los symlinks de los frameworks). La version del `Info.plist` deja de estar hardcodeada en 0.86 y sale del `CMakeLists`. Verificado: arranca con el entorno limpio, sin Qt de Homebrew. [ Deploy - El bundle de macOS pasa a ser autocontenido y se empaqueta ]

        - `REPO / EL .gitignore DEJABA ENTRAR CUALQUIER REGLA LOCAL DE CURSOR` la excepcion se escribia como `!.cursor/rules/`, que re-incluye el DIRECTORIO entero, no el archivo: alcanzaba con que alguien tuviera un `.mdc` propio ahi para que un `git add -A` lo subiera al repo. Fue lo que paso con `windows-build.mdc`, una regla local que termino publicada. Ahora la excepcion apunta al archivo (`.cursor/rules/*` + `!.cursor/rules/instructions.mdc`), asi que solo se versiona el espejo. Ademas se deja de trackear `VideoDownloader_installer.iss`: `instalador.bat` lo regenera entero en cada corrida y lo escribe con CRLF mientras `.gitattributes` lo pide en LF, asi que aparecia modificado despues de cada build aunque el contenido fuera identico. [ Repo - Acotar la excepcion de .cursor/rules y dejar de trackear el .iss generado ]

        - `BUILD / EL INSTALADOR EMPAQUETABA UN DEPLOY VIEJO SIN AVISAR` `instalador.bat` solo verificaba que la carpeta `deploy\` existiera, no que tuviera el ejecutable actual. Con un deploy anterior al rename adentro, empaqueto `VimeoDownloader.exe` mientras el `.iss` creaba los accesos directos apuntando a `{app}\VideoDownloader.exe`: la app quedaba instalada y sin poder abrirse desde ningun icono, y la corrida no reportaba un solo error. Ahora exige `deploy\VideoDownloader.exe` y aborta si `build\` es mas nuevo que lo desplegado, detectado con `xcopy /L /D`, que lista el origen solo cuando corresponderia copiarlo. Ademas `compilar.bat` y `deploy.bat` copiaban `tools\*.*`, lo que metia `sync_rules.py` —una utilidad del repo, no parte de la app— en el build y adentro del instalador; pasan a copiar solo `*.exe` y `*.dll`. [ Build - Verificar el deploy antes de empaquetar y no distribuir utilidades del repo ]

        - `ICONO MACOS / PASA A ICON COMPOSER` el icono del bundle se disena ahora en Icon Composer (`resources/icons/Alta/LGA_VideoDownloader_v003.icon`) y se compila con `actool`, en vez de generarse como `.icns` con el pipeline de `../LGA_IconLab`. Van **dos** recursos a `Contents/Resources/` y hacen falta los dos: `Assets.car`, que es el icono real y lo que consume macOS 26 via `CFBundleIconName`, y `AppIcon.icns`, fallback via `CFBundleIconFile`, generado con `--standalone-icon-behavior all` para que traiga los 10 tamanos (16..1024) en vez de los 4 del default, que llegan solo a 256 px. `CMakeLists.txt` copia ambos y avisa con `message(WARNING)` si falta el CAR; `cmake/Info.plist.in` suma `CFBundleIconName`. **Windows no se toca**: el `.ico` y su bloque siguen igual. De paso se arregla `compilar.sh`, que lanzaba la app en foreground y retenia la terminal: ahora va en background por defecto, con `--wait` para ver un crash o el exit code y `--no-run` para no lanzar — la convencion que ya tienen las otras apps LGA. Instrucciones completas del pipeline de iconos en la seccion "5.2 Icon Composer" de `../LGA_IconLab/docs/Doc_Iconos_App.md`. [ Icono - El icono de macOS pasa a Icon Composer, y compilar.sh no retiene la terminal ]

        - Fix de regresion: la entrada anterior daba por bueno "arreglar" el `prefix` del `.qrc` para que `dark_theme.qss` cargara. Cargo, y **rompio el layout entero**: campos superpuestos, group boxes sin padding y el input de URL reducido a una linea sobre el titulo. El `.qss` tiene 211 lineas con 38 reglas de `QGroupBox`/`QLineEdit` que quedaron desactualizadas respecto del codigo, y nunca se habian ejecutado. El estilo REAL de la app siempre fue el fallback `ColorUtils::getStyleSheet()`, que se aplicaba justamente porque el `open()` fallaba. Se revierte el `prefix` a `/styles` y se deja constancia en el `.qrc` y en `main.cpp` de que ese `open()` falla A PROPOSITO, para que nadie lo vuelva a "corregir": si algun dia se quiere revivir el `.qss`, primero se arregla el `.qss` y recien despues el `prefix`, no al reves. [ Fix - Revertir el prefix del qrc que rompia el layout ]

        - Rename: la app pasa a llamarse **VideoDownloader**. El nombre viejo era un misnomer — ya descargaba de Vimeo **y** YouTube. El barrido se hizo por formas exactas del producto (`VimeoDownloader`, `LGA_VimeoDownloader`, `vimeodownloader`, `Vimeo Downloader`), NO por lista de exclusion: asi las ~60 referencias al servicio quedan intactas por construccion, incluidas `vimeo.com` en la logica de formato, `isVimeoUrl()` y sobre todo las claves de settings `vimeo/username` y `vimeo/password`, que si se renombraran huerfanarian las credenciales guardadas. Se renombran 32 archivos de contenido, el directorio `include/vimeodownloader/` y los iconos. Los binarios de `tools/` y `toolsmac/` no se tocan: yt-dlp menciona `vimeo` en sus extractores internos. [ Rename - VimeoDownloader pasa a VideoDownloader ]

        - Migracion de AppData: el rename cambia la carpeta de settings, asi que sin migracion cada usuario abriria la version nueva sin sus credenciales de Vimeo ni su carpeta de descargas. `migrateLegacyAppDataDir()` en `main.cpp` copia `<AppData>/LGA/VimeoDownloader` a `.../VideoDownloader` en el primer arranque. El centinela es el ARCHIVO `config.ini`, no la existencia del directorio destino: `getConfigPath()` hace `mkpath` en cada llamada, asi que el directorio puede aparecer sin que la migracion haya corrido, y una guarda por directorio dejaria la migracion sin reintentar para siempre. Se copia a un staging y recien al final se renombra, para que un corte a mitad no deje datos parciales. La carpeta vieja NO se borra: son unos KB y queda como rollback. [ AppData - Migrar la config de VimeoDownloader a VideoDownloader ]

        - Instalador: el `.iss` no definia `AppId`, asi que Inno usaba el `AppName` como identificador y el rename habria creado una identidad nueva, dejando una entrada huerfana en Programas y caracteristicas apuntando a `C:\Portable\LGA\VimeoDownloader`. Se fija `AppId=VideoDownloader` explicito y se agrega limpieza de la clave `VimeoDownloader_is1` y de la carpeta de instalacion anterior. De paso `AppVersion` pasa de 0.86 a 0.89, que estaba desfasada del `project()` de CMake. Los cambios van en `instalador.bat`, que **regenera el `.iss` entero en cada corrida**; el archivo versionado lleva ahora un encabezado que lo aclara. [ Instalador - AppId explicito y limpieza de la instalacion anterior ]

        - Fixes de paso: el `.qrc` declaraba `prefix="/styles"` y el archivo como `styles/dark_theme.qss`, o sea que el recurso real era `:/styles/styles/dark_theme.qss` mientras `main.cpp` abria `:/styles/dark_theme.qss`. El tema oscuro nunca cargaba y la app caia siempre al fallback. Ademas la version estaba hardcodeada en `main.cpp`, duplicando el `project()` de CMake; ahora sale de la macro `VIDEODOWNLOADER_VERSION`. [ Fixes - Cargar el tema oscuro y tomar la version de CMake ]

        - Repo: el proyecto no tenia `.gitattributes` ni `.editorconfig`, asi que sin un default `* text=auto` Git guardaba los bytes tal cual llegaban y nunca normalizaba (8 archivos con CRLF en el indice conviviendo con 35 en LF, y los `.bat` sin garantia de CRLF). Se adopta la politica de los demas repos LGA: LF por defecto, CRLF solo en `.bat`/`.cmd`/`.ps1`, binarios marcados y `tools/`/`toolsmac/` excluidos con `-text`. Esa exclusion importa mas que en otros repos porque los ejecutables de macOS (`deno`, `ffmpeg`, `yt-dlp`) **no tienen extension**: ninguna regla por extension los cubre. La renormalizacion de 8 archivos se verifico con `--ignore-cr-at-eol` dando diff de contenido vacio, y va en commit propio con su SHA en `.git-blame-ignore-revs`. Se crean ademas los tres espejos de reglas, que el repo no tenia. [ Repo - Unificar line endings y crear las reglas del repo ]

        - Iconos: se adopta el diseño nuevo (`Alta/LGA_VideoDownloader_v003.png`), con el `.icns` y el `.ico` regenerados por el pipeline de `../LGA_IconLab` (`make_lga_icon.py --rim 0 --frac 0.70`). En macOS sigue la familia plana que ya usaba la app desde v0.87 — squircle conforme al 80% sin rim horneado, para que Tahoe agregue el suyo al renderizar. El `.ico` de Windows pasa a ser el **glyph flotando sobre transparente, sin el squircle**: ese fondo es exclusivo de macOS, donde el sistema exige una silueta conforme; en Windows quedaba como un recuadro oscuro pegado alrededor del logo. Además el `.ico` viejo tenía un solo tamaño (4 KB) y ahora trae los siete de 16 a 256 px, curados por el pipeline, así que la barra de tareas y el Explorer dejan de escalar un bitmap único. No hay cambios de código: esta app no llama a `setWindowIcon`, el icono de Windows sale del `.ico` incrustado en el `.exe`. [ Iconos - Adoptar el diseño v003 y sacarle el fondo al .ico de Windows ]

        - Build macOS: `compilar.sh`, `deploy.sh` y `limpiar.sh` dejaban de reiniciar el extension host de VSCode al ejecutarse. Los tres usaban `pkill -f VideoDownloader`, un patrón demasiado genérico: `-f` matchea contra la command line completa de todos los procesos y pegaba a extensiones/helpers de VSCode cuyo `--folder-uri` apunta a `LGA_VideoDownloader` (el nombre del workspace contiene la palabra), lo que provocaba que VSCode los relance varias veces al arrancar el script. Se reemplaza por `pkill -f "VideoDownloader.app/Contents/MacOS/VideoDownloader"` (path completo del ejecutable dentro del `.app`). Se agrega comentario preventivo en cada script para no volver al patrón genérico. [ Build macOS - Fix pkill genérico en compilar/deploy/limpiar que reiniciaba extensiones de VSCode ]

v0.89:
        - macOS: corrige el icono de Vimeo. En v0.88 el glyph había quedado mal (círculo+flecha chico, al ~50%, con un anillo fantasma del squircle viejo) porque la extracción por diferencia de fondo falló con el marco blanco del diseño original. Ahora se extrae por saturación (círculo CMY y flecha teal) y queda al **74%** limpio. Además el fondo pasa al **mismo gris que PipeSync** (gradiente vertical 46→28, nunca negro), consistente con el resto de las apps LGA. [ Mac - Fix icono Vimeo (glyph 74% + gris estandar) ]

v0.88:
        - macOS: se ajusta el tamaño del glyph dentro de `LGA_VideoDownloader.icns`. Quedaba grande (~89% del squircle); ahora va al **74%** con padding (como Codex/PipeSync), centrado sobre el plate plano (squircle 80% de Apple). [ Mac - Glyph al 74% del squircle ]

v0.87:
        - macOS: el icono de la app pasa al estándar glass — `.icns` squircle plano a la grilla exacta del 80% de Apple (esquinas continuas), sin rim horneado, dejando que macOS Tahoe agregue el borde de Liquid Glass al renderizar (como Photoshop/Cursor). Corrige el "icon jail" (recuadro gris en vistas de lista/detalle). Aprendizaje completo en `LGA_PipeSync_2/Docs/_Doc_Aprendizaje_QT_C.md`. [ Mac - Icono glass 80% + Tahoe rim ]

v0.86:
        - Version actual inicial registrada para empezar a mantener el changelog del repo bajo reglas `.cursor` y `.codex`. [ Reglas - Se inicializa changelog y reglas de repo ]

